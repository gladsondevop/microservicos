spring:
  profiles:
    active: docker
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/gladsondevop/server-config
          clone-on-start: true
          search-paths:
            - "config"
          username: gladsondevop
          password:
server:
  port: 8888

