spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/gladsondevop/server-config
          clone-on-start: true
          search-paths:
            - "config"
          username: gladsondevop
          password: ${GITHUB_CONFIG_PASSWORD}
server:
  port: 8888

